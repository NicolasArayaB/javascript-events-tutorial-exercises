// your function goes here
// Revisar, no logro encontrar el porque no carga el alert

//window.loadListener = function loadListener() {
//	document.body.addEventListener("load", () => alert("loading finished"));
//};

window.onload = function loadListener() {
	alert("loading finished");
};
